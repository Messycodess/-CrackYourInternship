class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """

        n=len(nums)
        m=n/2
        s=[]
        maxx=0
        for i in nums:
            if i not in s:
                s.append(i)
                c=nums.count(i)
                if c>m and c>maxx:
                    maxx=c
                    x=i
        return x          

    
           



        
